import React from 'react';
import { Typography } from '@mui/material';
const Logo = () => {
    return (
        <div>
            <Typography>Minet Logo</Typography>
        </div>
    );
};

export default Logo;