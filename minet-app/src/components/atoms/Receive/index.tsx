import React from 'react';
import { Typography } from '@mui/material';

const Grid = () => {
    return (
        <div>
            <Typography>Grid</Typography>
        </div>
    );
};

export default Grid;